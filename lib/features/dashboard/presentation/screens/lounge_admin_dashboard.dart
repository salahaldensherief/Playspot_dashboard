import 'package:flutter/material.dart';

class LoungeAdminDashboard extends StatelessWidget {
  const LoungeAdminDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Lounge Admin Dashboard'),
      ),
    );
  }
}
